apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: dremio

resources:
  - namespace.yaml
  - dremio-deployment.yaml  # Full enterprise deployment
  - ingress.yaml  # Enable after deployment is stable

# Common labels
commonLabels:
  app.kubernetes.io/part-of: data-platform
  app.kubernetes.io/managed-by: argocd
  app.kubernetes.io/name: dremio