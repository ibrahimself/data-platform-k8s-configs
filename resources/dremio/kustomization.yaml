apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: dremio

resources:
  - namespace.yaml
  - dremio-deployment.yaml  # Using OSS version, no auth needed
  # - image-pull-secret.yaml  # Not needed for OSS
  - ingress.yaml  # Disabled due to conflicts
  # - servicemonitor.yaml  # Enable after Dremio is running

# Common labels
commonLabels:
  app.kubernetes.io/part-of: data-platform
  app.kubernetes.io/managed-by: argocd
  app.kubernetes.io/name: dremio